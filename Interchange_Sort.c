void InterchangSort(int a[], int N)
{
    int i ,j;
    for(int i=0, i<N, i++)
        for(int j=i+1, j<=N, j++)
           if(a[j] < a[i])
              Hoanvi(a[i], a[j]);

              return 0;
}
